machine:
    environment:
        PATH: "$HOME/.cargo/bin:$PATH"

dependencies:
    pre:
        - curl https://sh.rustup.rs -sSf | sh -s -- --default-toolchain stable -y

test:
    override:
        - cargo build 
        - cargo build --release
        - cargo test
        - cargo test --release
